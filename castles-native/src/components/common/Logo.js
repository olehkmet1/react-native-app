import React, { Component } from 'react';

import { Image } from 'react-native';

export default () => (
    <Image style={{ height: 64, width:64 }} source={require('../../static/img/logo.png')} />
);
